import { PropsWithChildren,useEffect,useState } from 'react'
import { ChevronLeft,ChevronRight,Database,FileText,Home,LogOut,Plus,Sparkles } from 'lucide-react'
import { NavLink,useLocation,useNavigate } from 'react-router-dom'
import { createDatabase,createPage,getDatabases,getPages } from '../lib/data';import {supabase} from '../lib/supabase';import {useAppContext} from '../App';import type {Database as DatabaseType,Page} from '../types'
export default function AppShell({children}:PropsWithChildren){const{workspace}=useAppContext();const[databases,setDatabases]=useState<DatabaseType[]>([]),[pages,setPages]=useState<Page[]>([]),[collapsed,setCollapsed]=useState(()=>localStorage.getItem('atlas:sidebar-collapsed')==='true');const navigate=useNavigate(),location=useLocation();const refresh=async()=>{const[d,p]=await Promise.all([getDatabases(workspace.id),getPages(workspace.id)]);setDatabases(d);setPages(p)};useEffect(()=>{refresh().catch(console.error)},[workspace.id,location.pathname]);useEffect(()=>localStorage.setItem('atlas:sidebar-collapsed',String(collapsed)),[collapsed]);const addPage=async()=>{const p=await createPage(workspace.id,'Untitled page',pages.length);await refresh();navigate(`/page/${p.id}`)};const addDatabase=async()=>{const n=window.prompt('Database name?','Untitled database')?.trim();if(!n)return;const d=await createDatabase(workspace.id,n);await refresh();navigate(`/database/${d.id}`)};return <div className={`app-shell ${collapsed?'sidebar-is-collapsed':''}`}><aside className="sidebar"><div className="sidebar-collapse-row"><button className="sidebar-collapse-button" onClick={()=>setCollapsed(v=>!v)}>{collapsed?<ChevronRight size={17}/>:<ChevronLeft size={17}/>}</button></div><div className="brand-row"><div className="brand-mark small">A</div><div className="sidebar-copy"><strong>Atlas Studio</strong><span>{workspace.name}</span></div></div><nav className="nav-stack"><NavLink to="/" end><Home size={17}/><span className="sidebar-copy">Home</span></NavLink><div className="nav-label"><span className="sidebar-copy">Pages</span><button className="icon-button" onClick={addPage} title="New page"><Plus size={16}/></button></div>{pages.map(p=><NavLink key={p.id} to={`/page/${p.id}`} title={p.title}><FileText size={16}/><span className="truncate sidebar-copy">{p.title}</span></NavLink>)}<div className="nav-label database-label"><span className="sidebar-copy">Data</span><button className="icon-button" onClick={addDatabase} title="New database"><Plus size={16}/></button></div>{databases.map(d=><NavLink key={d.id} to={`/database/${d.id}`} title={d.name}><Database size={16}/><span className="truncate sidebar-copy">{d.name}</span></NavLink>)}</nav><div className="sidebar-footer">{!collapsed&&<div className="tiny-note"><Sparkles size={14}/> Pages are the experience. Data powers them.</div>}<button className="ghost-button full" onClick={()=>supabase.auth.signOut()}><LogOut size={16}/><span className="sidebar-copy">Sign out</span></button></div></aside><main className="main-area">{children}</main></div>}
