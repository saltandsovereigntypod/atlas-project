import { useEffect, useState } from 'react'
import { ArrowLeft, ArrowRight, Check, Compass, Database, Layers3, LayoutTemplate, MousePointer2, Palette, Sparkles, Type, WandSparkles, X } from 'lucide-react'

type Props={open:boolean;onClose:()=>void;onComplete?:()=>void}
const steps=[
 {icon:<Sparkles/>,title:'Atlas is a workspace builder',eyebrow:'START HERE',body:<><p>Atlas is built around one idea: <strong>freedom without chaos</strong>. Pages give you a visual canvas, databases keep information reusable underneath, and views let the same information appear differently wherever you need it.</p><p>You can build a calm personal dashboard, a project command center, a book library, a creative studio, a tracker, a grimoire, or something that does not fit a preset category at all.</p></>},
 {icon:<Compass/>,title:'Know the three big pieces',eyebrow:'DASHBOARD · PAGES · DATA',body:<><p><strong>Dashboard</strong> is the permanent home of your workspace. It is a real canvas, not a fixed summary screen, and it has its own dashboard templates.</p><p><strong>Pages</strong> are flexible canvases for focused spaces, projects, collections, notes, tools, and dashboards of their own.</p><p><strong>Data</strong> holds reusable collections such as Books, Episodes, Trips, Tasks, Characters, Transactions, or anything else you want to track. A database does not belong to one page. Pages decide where and how that data appears.</p></>},
 {icon:<MousePointer2/>,title:'Edit mode builds the machine',eyebrow:'EDIT MODE',body:<><p>Open a page and enter <strong>Edit</strong> when you want to change layout, appearance, bindings, or structure. Click an object to select it. Drag it to move it, use the resize handles to change its size, and open <strong>Style</strong> for the contextual inspector.</p><p>The bottom tool dock gives you <strong>Add</strong>, <strong>Layers</strong>, <strong>Assets</strong>, and the template <strong>Library</strong>. The zoom control supports click-and-hold, so you can move smoothly between detail work and the full page.</p><p>Delete or Backspace removes a selected object. Arrow keys nudge it. Shift + Arrow moves farther. Ctrl/Cmd + D duplicates it. Layers controls what sits in front of what.</p></>},
 {icon:<Palette/>,title:'Style the actual pieces',eyebrow:'COLOR · SHAPE · EFFECTS',body:<><p>Atlas treats a designed object as a collection of visual parts instead of one giant style setting. A widget can have separate colors for its surface, text, border, inputs, input text, input borders, buttons, button text, icons, checkboxes, checkbox borders, checkmarks, and other meaningful pieces.</p><p>The color dots show the current color of each editable channel. Click one to open the color picker, use page colors or recent colors, pick a manual color, use the eyedropper when your browser supports it, or build a gradient where gradients make sense.</p><p><strong>Shape & Effects</strong> controls things like corner radius, opacity, border width, and border style. If you can visually point at a distinct part of an object, Atlas is moving toward letting you style that part independently.</p></>},
 {icon:<Type/>,title:'Typography belongs to the object',eyebrow:'FONTS & TYPE',body:<><p>Text-bearing objects can have their own font family, size, weight, line height, letter spacing, and alignment. You are not limited to one page-wide typeface.</p><p>The font picker includes common fonts, uploaded workspace fonts, and any installed font name you want to use. Upload font files through <strong>Assets</strong> when you want them saved with the workspace and reusable across pages.</p></>},
 {icon:<Layers3/>,title:'Build from reusable pieces',eyebrow:'ADD · LAYERS · ASSETS',body:<><p><strong>Add</strong> is where you place headings, text, documents, media, buttons, sections, widgets, database views, metrics, progress elements, and other page pieces.</p><p><strong>Layers</strong> is the structural view of the canvas. Use it when objects overlap, when something is hard to click, or when you need to bring an element forward, send it backward, hide it, lock it, duplicate it, or delete it.</p><p><strong>Assets</strong> is your reusable workspace library for images, files, audio, and custom fonts. Upload once, then place the same asset wherever you need it.</p></>},
 {icon:<LayoutTemplate/>,title:'Templates are complete starting systems',eyebrow:'LIBRARY',body:<><p>The Library is divided into <strong>Pages</strong>, <strong>Sections</strong>, <strong>Styles</strong>, and <strong>Data</strong>. Page templates can replace the current composition or be added to it. Sections add useful clusters without replacing the page. Styles restyle what already exists. Data tools help you create and connect collections.</p><p>When you are editing the permanent Dashboard, Atlas also shows a dedicated <strong>Dashboard templates</strong> section. Those layouts are designed as broad home bases rather than copies of focused page templates.</p><p>If a template needs data, Atlas shows you what it needs and lets you connect an existing database, create the required collection, or leave it unconnected. Important data relationships should never be silently guessed.</p></>},
 {icon:<Database/>,title:'Databases store, views present',eyebrow:'DATA MODEL',body:<><p>A database is a collection of records. A Books database might contain one record for each book, while properties store things such as Author, Status, Rating, Cover, Started, Finished, and Notes.</p><p>The same record can appear in several places without being copied. A Currently Reading view, a full library, and a dashboard card can all point to the same book. Edit the record once and every connected view can reflect the change.</p><p>Use the main Data area when you want to change the collection itself, including properties, records, and structure. Use the page inspector when you want to change how that data is presented on one particular canvas.</p></>},
 {icon:<LayoutTemplate/>,title:'Views are perspectives on the same data',eyebrow:'TABLE · BOARD · GALLERY · CANVAS',body:<><p>Table, Board, Gallery, List-style layouts, and Canvas Cards are presentations of data, not separate databases. Each view can decide what properties appear, how records are sorted or filtered, and how the layout behaves.</p><p><strong>Canvas Cards</strong> are the most visual option. They let a database record behave like a designed card with individually placed elements. The goal is for the card itself to feel like a miniature canvas while the underlying record stays connected to the database.</p></>},
 {icon:<WandSparkles/>,title:'Widgets are interactive page tools',eyebrow:'FUNCTION + DELIGHT',body:<><p>Widgets can be clocks, calendars, checklists, focus tools, notes, counters, goals, progress indicators, capture tools, database summaries, creative utilities, and more.</p><p>Some widgets store simple information inside themselves. Others connect to a database. In normal use, interactive widgets should be usable directly from the page. Edit mode is for designing the tool. Use mode is for actually using it.</p><p>Widget styling is intentionally granular. As Atlas grows, internal controls such as inputs, buttons, checkboxes, labels, icons, tracks, and states should remain independently customizable rather than collapsing back into one generic widget style.</p></>},
 {icon:<Check/>,title:'Use mode operates what you built',eyebrow:'EVERYDAY USE',body:<><p>You should not need to enter Edit just to live in your workspace. In normal mode, use the dashboard, capture information, check tasks, update statuses, interact with widgets, open records, and work with the systems you designed.</p><p>There is no required build order. Start blank, use a dashboard template, apply a page template, add one section, create a database first, or simply place objects until the page begins to make sense.</p><p>If you get lost, click the thing you are trying to change. Atlas should reveal controls that belong to that object, rather than making you hunt through a giant settings screen. You can reopen this guide at any time from <strong>Guide</strong> in the sidebar.</p><p><strong>The goal is not to make every workspace look the same. The goal is to give you enough structure and freedom to build the workspace you actually want.</strong></p></>}
]
export default function AtlasTutorial({open,onClose,onComplete}:Props){const[index,setIndex]=useState(0);useEffect(()=>{if(open)setIndex(0)},[open]);if(!open)return null;const step=steps[index],last=index===steps.length-1;const finish=()=>{onComplete?.();onClose()};return <div className="atlas-tutorial-backdrop" role="dialog" aria-modal="true" aria-label="Atlas guide"><div className="atlas-tutorial"><button className="atlas-tutorial-close" onClick={onClose} aria-label="Close guide"><X/></button><aside><div className="atlas-tutorial-brand">A</div><span>ATLAS GUIDE</span><h2>Build the workspace<br/>you actually want.</h2><div className="atlas-tutorial-progress">{steps.map((_,i)=><button key={i} className={i===index?'active':i<index?'done':''} onClick={()=>setIndex(i)} aria-label={`Go to guide step ${i+1}`}><span>{i<index?'✓':i+1}</span><small>{steps[i].eyebrow}</small></button>)}</div></aside><main><div className="atlas-tutorial-icon">{step.icon}</div><span className="atlas-tutorial-eyebrow">{step.eyebrow}</span><h1>{step.title}</h1><div className="atlas-tutorial-copy">{step.body}</div><div className="atlas-tutorial-actions"><button disabled={index===0} onClick={()=>setIndex(i=>i-1)}><ArrowLeft/>Back</button><span>{index+1} of {steps.length}</span>{last?<button className="primary" onClick={finish}>Start creating <Check/></button>:<button className="primary" onClick={()=>setIndex(i=>i+1)}>Next <ArrowRight/></button>}</div></main></div></div>}
